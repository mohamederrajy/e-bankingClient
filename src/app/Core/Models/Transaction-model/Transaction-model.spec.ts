import {TransactionModel} from './Transaction-model.component';
describe('DashboradModel', () => {
  it('should create an instance', () => {
    expect(new TransactionModel()).toBeTruthy();
  });
});
