import {BeneficiaireModel, } from './beneficiaire-model.component';


describe('BeneficiaireModel', () => {
  it('should create an instance', () => {
    expect(new BeneficiaireModel()).toBeTruthy();
  });
});
