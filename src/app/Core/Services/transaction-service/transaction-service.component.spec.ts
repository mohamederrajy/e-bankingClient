import { TransactionService} from './transaction-service.component';


describe('BeneficiaireModel', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new TransactionService()).toBeTruthy();
  });
});
