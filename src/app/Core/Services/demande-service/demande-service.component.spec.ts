import { DemandeService} from './demande-service.component';


describe('BeneficiaireModel', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new DemandeService()).toBeTruthy();
  });
});
