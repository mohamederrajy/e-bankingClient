import {AgenceService} from './beneficiare-service.component';


describe('BeneficiaireModel', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new AgenceService()).toBeTruthy();
  });
});
